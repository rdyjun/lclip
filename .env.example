# ── Server ────────────────────────────────────────────────────────────────────
PORT=3000

# ── GitHub Container Registry ─────────────────────────────────────────────────
# Replace with your GitHub username / org (lowercase).
GITHUB_USER=rdyjun

# ── NAS storage paths (host-side volume mounts) ───────────────────────────────
# These are the directories ON YOUR NAS that docker-compose will mount.
# Examples for Synology DSM:
#   NAS_DATA_DIR=/volume1/docker/lclip/data
#   NAS_UPLOADS_DIR=/volume1/docker/lclip/uploads
#   NAS_EXPORTS_DIR=/volume1/docker/lclip/exports
#
# Examples for generic Linux NAS:
#   NAS_DATA_DIR=/mnt/storage/lclip/data
#   NAS_UPLOADS_DIR=/mnt/storage/lclip/uploads
#   NAS_EXPORTS_DIR=/mnt/storage/lclip/exports
#
# Default (local dev): uses ./data  ./uploads  ./exports relative to this file.
NAS_DATA_DIR=./data
NAS_UPLOADS_DIR=./uploads
NAS_EXPORTS_DIR=./exports
